package main

import (
	"fmt"
)

var x int
var y string
var z bool

func main() {

	fmt.Println(x, y, z) // Esses valores atribuidos as variáveis se chamam valor zero, que é o valor atribuido a variáveis não inicializadas, apenas declaradas.
}
